<h1>Grupo de Usuários</h1>

<div class="actions">
  <%= link_to "Novo", new_user_group_path %>
</div>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Ativo</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= link_to group.name, group %></td>
        <td><%= group.active %></td>
        <td><%= link_to "Editar", edit_user_group_path(group) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
